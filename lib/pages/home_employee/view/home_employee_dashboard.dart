import 'package:flutter/material.dart';

class HomeEmployeeDashboard extends StatelessWidget {
  const HomeEmployeeDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
      
    );
  }
}
